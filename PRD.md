# MangaFind Product Requirements Document

## 1. 문서 개요

### 1.1 제품명

MangaFind (가칭)

### 1.2 제품 정의

MangaFind는 SNS에서 발견한 일본 만화 캡처 이미지를 업로드하면 작품을 식별하고, 한국 사용자가 이해할 수 있는 제목과 정발 정보를 제공하는 모바일 우선 PWA다.

### 1.3 문서 목적

이 문서는 MangaFind V1의 제품 목표, 사용자 경험, 기능 범위, 성공 기준과 검증 조건을 정의한다. API 구현, 모델 SDK, 이미지 처리 라이브러리, Docker, Azure 리소스 설정은 TRD에서 정의한다.

### 1.4 기준 문서

- `기획서.md`: 제품 아이디어와 전체 범위
- `PRD.md`: 제품 요구사항과 사용자 검증 기준
- `TRD.md`: 기술 설계와 구현 계약

## 2. 문제와 기회

### 2.1 해결할 문제

한국 사용자가 SNS에서 일본 만화를 발견했을 때 다음 과정을 직접 반복해야 한다.

```text
캡처
→ 일본어 확인
→ 일본어 검색
→ 작품 후보 조사
→ 한국어 제목 검색
→ 한국 정발 여부 검색
→ 작품 정보 확인
```

사용자는 이미지 속 일본어를 직접 입력하기 어렵고, 일본 원제를 찾아도 한국 정식 제목이나 국내 통용명을 다시 찾아야 한다.

### 2.2 제품 기회

사용자가 만화 캡처 이미지만 제공하면 이미지 분석, 일본 작품 검색, 한국 정보 조사를 하나의 흐름으로 제공할 수 있다. MangaFind의 핵심 차별점은 단순한 이미지 추측이 아니라 이미지 단서와 실제 검색 결과를 결합하는 것이다.

## 3. 목표와 비목표

### 3.1 V1 목표

비회원 사용자가 일본 만화 캡처 이미지 1~3장을 업로드하고 다음 정보를 확인할 수 있게 한다.

- 일본 작품 후보 TOP 3
- 일본 원제
- 작가
- 한국어 제목 또는 참고 번역
- 한국어 제목 상태
- 한국 정발 여부 상태
- 작품 식별 판정 수준과 판단 근거

### 3.2 V1 성공 정의

V1의 성공은 기능 개수가 아니라 “캡처 이미지로 찾고 싶은 작품을 후보 안에 포함시키는가”로 판단한다.

- Top 1 작품 식별 정확도: 70% 이상
- Top 3 후보 내 정답 포함률: 85% 이상
- `HIGH` 판정 후보의 오판률: 15% 이하
- 한국어 제목 상태 판정 정확도: 70% 이상
- 전체 요청 p95 처리 시간: 60초 이하
- 평균·최대 요청 비용: 사전 측정 후 승인된 운영 예산 이하

성공 기준은 이미지 1장, 2장, 3장 사례를 포함한 최소 20개 이상, 권장 30개 샘플로 평가한다.

### 3.3 V1 비목표

다음 기능은 V1에서 구현하지 않는다.

- 회원가입 및 로그인
- 검색 기록, 찜, 내 만화 목록, 기기 간 동기화
- 사용자 이미지 영구 저장
- 자체 OCR 모델 또는 자체 검색엔진
- 복잡한 웹 크롤러와 별도 검색 API
- 사용자 피드백의 DB 저장 또는 모델 학습
- 관리자 페이지
- 작품 표지, 장르, 권수, 발매일, 연재 상태 자동 수집을 위한 별도 데이터 시스템
- 사용자에게 검색 웹 출처 목록을 보여주는 기능

## 4. 대상 사용자

### 4.1 주요 사용자

일본 만화에 관심이 있으며 SNS에서 발견한 작품의 제목과 한국 정보를 빠르게 확인하려는 한국어 사용자다.

### 4.2 사용자 특성

- 일본어를 읽지 못하거나 직접 입력하기 어렵다.
- 모바일에서 SNS를 사용하다가 바로 검색한다.
- 작품명을 정확히 몰라도 이미지 한 장으로 검색을 시작하고 싶어 한다.
- AI 답변보다 실제 작품 정보에 근거한 결과를 원한다.
- 한국 정식 제목과 비공식 통용명을 구분할 필요가 있다.

## 5. 핵심 사용자 시나리오

### 5.1 주 시나리오: 작품 찾기

1. 사용자가 SNS에서 일본 만화 장면을 발견한다.
2. 사용자가 장면을 1~3장 캡처한다.
3. 사용자가 MangaFind에 접속해 이미지를 선택하거나 촬영한다.
4. 사용자가 외부 AI 전송 안내를 확인하고 분석을 시작한다.
5. 시스템이 이미지에서 일본어와 시각적 단서를 추출한다.
6. 시스템이 일본 작품 후보 TOP 3을 검색한다.
7. 시스템이 후보별 한국어 제목과 정발 정보를 검색한다.
8. 시스템이 검색 결과와 단서를 최종 비교한다.
9. 사용자가 대표 후보, 다른 후보, 한국 정보와 판정 근거를 확인한다.

### 5.2 정보 부족 시나리오

이미지에서 작품을 특정할 단서가 부족하면 임의의 작품을 생성하지 않는다.

화면 안내:

> 이 캡처에서는 작품을 특정할 만한 충분한 단서를 찾지 못했습니다. 대사나 캐릭터 이름이 잘 보이는 이미지를 추가해 주세요.

### 5.3 부분 성공 시나리오

일본 작품 후보는 찾았지만 한국 정보 조사가 완료되지 않은 경우다.

화면 규칙:

- 일본 작품 후보와 일본 원제는 표시한다.
- 한국어 제목은 `한국어 제목 확인 불충분`으로 표시한다.
- 정발 여부는 `확인 불충분`으로 표시한다.
- 결과 상단에 “일본 작품은 찾았지만 한국 정보 조사가 완료되지 않았습니다.”를 표시한다.
- 다른 후보를 선택하면 해당 후보의 한국 정보 상태도 개별적으로 표시한다.

## 6. 제품 원칙

### 6.1 검색 우선

AI가 이미지만 보고 작품을 확정하지 않는다.

```text
이미지 분석
→ 단서 추출
→ 실제 웹 검색
→ 후보 비교
→ 최종 판정
```

### 6.2 불확실성 표시

검색 결과가 부족하거나 서로 충돌하면 결과를 채워 넣지 않는다. 사용자는 확정 정보, 참고 정보, 확인 불충분 정보를 구분할 수 있어야 한다.

### 6.3 제목 상태 구분

한국어 제목 상태와 정발 여부는 별도로 표시한다.

- `OFFICIAL`: 한국 공식 제목과 정식 발매가 함께 확인됨
- `COMMON`: 공식 제목은 확인되지 않았지만 서로 다른 2종류 이상의 한국어 자료에서 같은 통용명이 확인됨
- `TRANSLATED`: 일본 원제는 확인됐고 AI 참고 번역은 가능하지만 공식명·통용명은 확인되지 않음
- `UNKNOWN`: 원제 또는 한국어 제목을 신뢰할 수 없거나 검색 결과가 부족·충돌·실패함

`TRANSLATED`는 반드시 `AI 번역` 또는 `참고 제목`으로 표시한다.

정발 여부는 다음 중 하나다.

- `CONFIRMED`: 한국 출판사 또는 정식 판매 정보로 확인됨
- `NOT_FOUND`: 조사 범위에서 정식 발매 정보를 찾지 못함
- `UNKNOWN`: 검색 결과가 부족하거나 서로 일치하지 않음

### 6.4 사용자에게 출처 목록 미표시

검색 결과의 근거와 인용 메타데이터는 내부 최종 판정에 사용한다. 단, V1 결과 화면에는 웹 출처 목록이나 링크를 표시하지 않는다. 출처를 확보하지 못한 정보는 확정하지 않고 확인 불충분 상태로 표시한다.

## 7. 기능 요구사항

### FR-01. 이미지 업로드

사용자는 JPG, JPEG, PNG, WebP 이미지를 1~3장 업로드할 수 있어야 한다.

입력 제한:

- 파일당 최대 10MB
- 이미지 가로·세로 각각 최대 8192px
- 이미지 1장당 최대 64MP
- 파일 확장자와 MIME만 믿지 않고 실제 형식과 디코딩 가능 여부를 서버에서 재검증
- 모든 이미지가 유효해야 분석을 시작할 수 있음

### FR-02. 업로드 전 개인정보 안내

분석 시작 전 다음 내용을 사용자에게 안내해야 한다.

- 업로드 이미지는 외부 AI 서비스로 전송될 수 있음
- SNS 계정명, 얼굴, 댓글, 위치 등 개인정보가 포함될 수 있음
- 분석 처리 후 서버에 원본을 영구 저장하지 않음
- 민감한 정보가 포함된 이미지는 업로드하지 않도록 권장

### FR-03. 이미지 분석

Gemini 2.5 Pro가 이미지 1~3장을 분석해 다음 단서를 구조화한다.

- 일본어 대사와 문자열
- 작품명으로 추정되는 문자열
- 등장인물 이름
- 작가명
- 출판사·연재처 단서
- 이미지 속 로고와 시각적 단서
- SNS 게시물 텍스트나 해시태그가 이미지에 포함된 경우의 관련 문자열

이 단계는 작품을 확정하지 않고 검색 단서를 반환한다.

### FR-04. 일본 작품 후보 검색

Gemini 2.5 Flash와 Google Search를 사용해 이미지 분석 결과로 일본 작품 후보를 검색한다.

- 후보는 최대 3개다.
- 후보는 검색 결과와 이미지 단서의 일치 정도로 정렬한다.
- 후보마다 일본 원제, 작가, 판단 근거를 반환한다.
- 근거가 1개도 없는 후보는 반환하지 않는다.

### FR-05. 한국 정보 검색

Gemini 2.5 Flash와 Google Search를 사용해 일본 작품 후보 TOP 3의 한국 정보를 한 번의 검색 단계에서 조사한다.

후보별로 다음 정보를 반환한다.

- 한국 공식 제목
- 국내 통용명
- AI 참고 번역
- 한국 정발 여부
- 정보 확인 상태

후보별 정보가 다르게 확인될 수 있으며, 한 후보의 조사 실패가 다른 후보의 성공을 무효화하지 않는다.

### FR-06. 최종 판정

Gemini 2.5 Pro가 다음 구조화된 결과를 입력으로 받아 최종 후보를 판정한다.

- 이미지 분석 JSON
- 일본 검색 후보 TOP 3
- 후보별 한국 정보 검색 결과
- 내부 검색 근거 메타데이터

최종 판정 모델은 원본 이미지를 다시 받지 않으며 새로운 웹 검색을 수행하지 않는다. 입력 결과에 없는 사실을 추가하지 않는다.

### FR-07. 결과 표시

정상 결과는 다음 정보를 표시해야 한다.

- 대표 후보의 한국어 제목
- 제목 상태 배지
- 일본 원제
- 작가
- 한국 정발 여부
- 작품 판정 수준: 높음·중간·낮음
- 판단 근거
- 다른 후보 TOP 2·TOP 3

작품 표지, 장르, 출판사, 연재처, 연재 상태는 확인된 경우에만 선택적으로 표시한다.

### FR-08. 결과 상태

#### SUCCESS

일본 작품 식별과 한국 핵심 정보 조사가 완료된 상태다.

#### PARTIAL_SUCCESS

2회 호출 구조에서 ②가 후보를 반환했지만 일부 후보의 한국어 제목 또는 정발 여부가 `UNKNOWN`인 상태다. 후보는 표시하되 확인되지 않은 필드는 `UNKNOWN`으로 표시한다.

#### INSUFFICIENT

이미지 단서 또는 일본 작품 후보가 부족한 상태다. 후보 배열은 비어 있어야 한다.

#### FAILED

외부 서비스 오류, 응답 검증 실패 또는 timeout으로 핵심 결과를 만들지 못한 상태다. 후보 배열은 비어 있어야 한다.

### FR-09. 사용자 피드백

결과 화면에 다음 피드백 UI를 제공할 수 있다.

- 이 작품이 맞아요
- 다른 작품이에요

V1에서는 피드백을 DB에 저장하거나 모델 학습에 사용하지 않는다.

### FR-10. PWA와 반응형 사용성

- 모바일 웹에서 핵심 흐름을 사용할 수 있어야 한다.
- PC 웹에서도 파일 업로드와 결과 확인이 가능해야 한다.
- PWA 설치에 필요한 기본 manifest와 앱 실행 구성을 제공한다.
- 오프라인에서 AI 검색을 수행하는 기능은 제공하지 않는다.

## 8. 단계별 처리 정책

### 8.1 처리 순서

```text
이미지 검증
→ ① Gemini 이미지 분석 + Google Search grounding
→ ② Gemini 최종 판정: 일본 TOP 3 + 한국 정보
→ 응답 검증 및 결과 표시
```

②는 ①의 검증된 OCR·단서·검색 결과가 필요하므로 두 호출은 순차 실행한다.

### 8.2 시간 정책

- 각 외부 모델 호출: 최대 12초
- 전체 요청: 최대 60초
- 서버 자동 재시도: 없음
- 오류나 timeout 발생 시 사용자가 새 요청으로 다시 시도

실제 업로드 시간, 단계별 처리 시간, 전체 p95 시간은 출시 전 벤치마크로 측정한다.

### 8.3 비용 정책

기본 요청은 다음 2회 호출로 계산한다.

```text
이미지 분석·검색 grounding 1회
+ 최종 후보·한국 정보 판정 1회
```

각 요청에서 모델별 토큰, Search 사용 여부, 실제 호출 횟수, 처리 시간, 결과 상태를 기록한다. 실제 비용은 API 키와 결제 프로젝트가 연결된 환경에서 측정하기 전까지 확정하지 않는다.

## 9. 비기능 요구사항

### NFR-01. 보안

- Gemini API 키는 클라이언트에 노출하지 않는다.
- 서버에서 이미지 형식과 크기를 재검증한다.
- EXIF 등 불필요한 메타데이터를 제거한다.
- 원본 이미지는 요청 처리 후 삭제하고 영구 저장하지 않는다.
- 로그에 이미지 원본, API 키, 내부 프롬프트를 기록하지 않는다.

### NFR-02. 비용 보호

- IP당 분당 최대 3회
- IP당 1일 최대 30회
- 동시 처리 요청 제한
- HTTP 429와 `Retry-After` 반환
- 사용량 또는 예산 초과 시 신규 요청 제한

### NFR-03. 접근성 및 사용성

- 모바일 화면에서 업로드와 결과 확인이 가능해야 한다.
- 로딩 중 현재 처리 단계와 예상 상태를 표시해야 한다.
- 실패·정보 부족·부분 성공 상태를 빈 화면으로 표시하지 않는다.
- 색상만으로 제목 상태나 오류 상태를 구분하지 않는다.

## 10. 수용 기준

### AC-01. 업로드

- [ ] 유효한 이미지 1장 업로드가 가능하다.
- [ ] 유효한 이미지 3장 업로드가 가능하다.
- [ ] 4장 이상, 10MB 초과, 64MP 초과, 지원하지 않는 형식은 거부된다.
- [ ] 손상되거나 실제 디코딩할 수 없는 파일은 거부된다.

### AC-02. 정상 식별

- [ ] 이미지 분석, 일본 검색, 한국 검색, 최종 판정 단계가 순서대로 실행된다.
- [ ] 일본 작품 후보는 최대 3개다.
- [ ] 대표 후보에 일본 원제와 작가가 표시된다.
- [ ] 한국어 제목 상태와 정발 여부가 함께 표시된다.
- [ ] 판단 근거가 1개 이상 표시된다.

### AC-03. 부분 성공과 실패

- [ ] 일본 작품 식별 성공·한국 정보 실패 시 `PARTIAL_SUCCESS`로 표시된다.
- [ ] 부분 성공 시 한국어 제목은 `확인 불충분`으로 표시된다.
- [ ] 이미지 단서 부족 시 후보를 임의로 생성하지 않는다.
- [ ] 핵심 단계 실패 시 빈 후보 목록과 재시도 안내를 표시한다.

### AC-04. 제목 상태

- [ ] 공식 제목은 `OFFICIAL`로 표시된다.
- [ ] 서로 다른 2종류 이상 한국어 자료에서 반복된 제목은 `COMMON`으로 판정할 수 있다.
- [ ] AI 번역만 있는 제목은 `TRANSLATED`와 `AI 번역` 표시를 함께 사용한다.
- [ ] 제목을 신뢰할 수 없으면 `UNKNOWN`과 확인 불충분 표시를 사용한다.

### AC-05. 시간과 비용

- [ ] 전체 요청 p95 시간이 60초 이하인지 측정한다.
- [ ] 이미지 1장·2장·3장 요청을 각각 측정한다.
- [ ] 모델별 토큰과 Search 사용량을 기록한다.
- [ ] 평균·최대 비용이 승인된 운영 예산 이하인지 확인한다.

### AC-06. 개인정보와 보안

- [ ] 외부 AI 전송 안내가 분석 시작 전에 표시된다.
- [ ] Gemini API 키가 브라우저에 노출되지 않는다.
- [ ] 분석 완료·실패·timeout 후 원본 이미지가 영구 저장되지 않는다.
- [ ] 로그에 원본 이미지와 민감 정보가 기록되지 않는다.

## 11. 평가 계획

### 11.1 정확도 평가 데이터

최소 20개, 권장 30개의 테스트 샘플을 준비한다.

- 이미지 1장, 2장, 3장 사례 포함
- 일본어 대사가 있는 사례
- 캐릭터 이름만 있는 사례
- 작품 로고가 있는 사례
- 텍스트가 거의 없는 사례
- 정발 작품과 미정발 작품
- 한국 통용명이 있는 작품과 없는 작품

각 샘플에는 기대 일본 작품, 기대 한국어 제목 상태, 기대 정발 여부를 사전에 기록한다.

### 11.2 비용·성능 측정

30건의 실제 요청에서 다음을 측정한다.

- 업로드 시간
- 단계별 시작·종료 시각
- 전체 처리 시간 p50·p95
- Gemini Pro·Flash 입력·출력 토큰
- Google Search 사용량
- 결과 상태별 비율
- 요청당 평균·최대 비용

기준 미달 시 이미지 해상도, 후보 수, 프롬프트 입력 크기 또는 모델 조합을 조정하고 동일한 방식으로 재측정한다.

## 12. 출시 보류 조건

다음 중 하나라도 해당하면 V1 공개 출시를 보류한다.

- Top 3 정답 포함률이 85% 미만
- `HIGH` 판정 오판률이 15% 초과
- 전체 p95 응답 시간이 60초 초과
- 승인된 운영 예산을 초과
- API 키가 클라이언트에 노출
- 업로드 원본이 영구 저장됨
- 부분 성공·정보 부족 상태가 사용자에게 구분되지 않음

## 13. V2 후보

V1의 정확도와 비용이 검증된 후 다음 기능을 검토한다.

- 검색 기록과 로그인
- 즐겨찾기와 내 만화 목록
- 기기 간 동기화
- 사용자 피드백 저장
- 검색 출처 표시
- 대체 검색 API
- 비동기 작업과 진행 상태 조회
- 작품 표지·장르·연재 상태·권수·발매일

## 14. 미결정 및 검증 항목

다음 항목은 PRD의 요구사항을 바꾸지 않으며 TRD와 벤치마크에서 검증한다.

- Gemini SDK와 API 호출 방식의 구체 버전
- Google Search Grounding의 프로젝트별 할당량과 실제 과금
- 이미지 디코딩 및 EXIF 제거 라이브러리
- Azure Container Apps의 CPU·메모리·확장 설정
- ④ 최종 판정 입력 JSON의 최대 크기
- 실제 모바일 네트워크에서 업로드와 전체 p95 시간

