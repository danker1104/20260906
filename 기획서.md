1. 프로젝트 개요

프로젝트명(가칭)
MangaFind

프로젝트 형태
AI 기반 일본 만화 검색 PWA 웹앱

프로젝트 목표

SNS 릴스, 쇼츠, 게시물 등에서 우연히 발견한 일본 만화의 제목을 알기 어려운 문제를 해결한다.

사용자가 만화가 포함된 스크린샷을 업로드하면 AI가 이미지 속 일본어와 시각적 단서를 분석하고 웹 검색을 활용하여 작품을 식별한다.

단순히 일본 원제만 알려주는 것이 아니라 한국 사용자에게 필요한 한국 정식 제목 또는 국내 통용 제목, 한국 정발 여부, 작가, 출판사, 연재처 등의 정보까지 함께 제공한다.

2. 문제 정의

Instagram Reels, YouTube Shorts, X 등의 SNS에서는 일본 만화의 일부 장면이 자주 공유된다.

하지만 다음과 같은 문제가 있다.

작품 제목이 표시되지 않는 경우가 많음
제목이 일본어라 직접 검색하기 어려움
이미지 속 일본어를 직접 입력하기 어려움
대사만 존재하는 경우 작품을 찾기 어려움
일본 원제를 찾아도 한국에서 어떤 이름으로 알려졌는지 다시 검색해야 함
한국 정발 여부를 별도로 찾아야 함
미정발 작품은 한국에서 사용하는 통용 제목을 찾기가 어려움

결국 사용자는

캡처
 ↓
일본어 확인
 ↓
일본어 검색
 ↓
작품 후보 조사
 ↓
한국어 제목 검색
 ↓
정발 여부 검색
 ↓
작품 정보 검색

과정을 직접 반복해야 한다.

MangaFind는 이 과정을 하나로 통합한다.

3. 핵심 해결 방법

사용자는 만화 캡처 이미지만 제공한다.

시스템이 나머지 검색 과정을 자동으로 수행한다.

SNS에서 만화 발견
        ↓
스크린샷
        ↓
MangaFind 업로드
        ↓
AI 이미지 분석
        ↓
일본어 및 작품 단서 추출
        ↓
웹 검색
        ↓
작품 후보 탐색
        ↓
후보 검증
        ↓
일본 작품 식별
        ↓
한국 웹 추가 조사
        ↓
한국어 제목 / 정발 정보 확인
        ↓
최종 작품 정보 제공

핵심 원칙은 AI가 이미지만 보고 작품을 추측하도록 하지 않는 것이다.

이미지 분석 → 단서 추출 → 실제 웹 검색 → 후보 비교 → 결과 검증

방식을 사용한다.

4. 주요 사용자
주요 대상

일본 만화에 관심이 있으며 SNS에서 발견한 작품의 정보를 빠르게 찾고 싶은 한국 사용자.

특히 다음 상황을 주요 사용 사례로 한다.

"릴스 보다가 재밌어 보이는 만화가 나왔는데 제목이 일본어라 뭔지 모르겠다."

사용자는 일본어를 몰라도 사용할 수 있어야 한다.

5. 앱 형태

MangaFind는 네이티브 Android/iOS 앱이 아닌 PWA(Progressive Web App) 로 개발한다.

처음에는 웹사이트로 접속한다.

MangaFind 웹사이트 접속

        ↓

홈 화면에 추가 / 앱 설치

        ↓

📱 MangaFind

        ↓

일반 앱과 유사하게 실행

이를 통해 별도의 앱스토어 배포 없이 휴대폰에 설치하여 사용할 수 있도록 한다.

PC 웹에서도 사용할 수 있다.

6. 핵심 기능
6.1 캡처 이미지 업로드

사용자는 SNS에서 캡처한 이미지를 업로드한다.

V1 기준
JPG
JPEG
PNG
WebP
최대 3장

이미지 제한:

- 파일당 최대 10MB
- 이미지 최대 가로/세로 8192px
- 이미지 1장당 최대 총 픽셀 수 64MP
- 서버에서 파일 확장자와 MIME만 믿지 않고 실제 이미지 형식과 디코딩 가능 여부를 재검증한다.

여러 장의 이미지를 지원하는 이유는 서로 다른 이미지에서 다른 단서를 얻을 수 있기 때문이다.

예:

캡처 #1
→ 캐릭터 대사

캡처 #2
→ 캐릭터 이름

캡처 #3
→ 작품 로고

       ↓

세 이미지의 단서를 통합

       ↓

검색 정확도 향상
7. AI 이미지 분석

업로드된 이미지는 Gemini 이미지 모델을 이용해 분석한다.

① Gemini 이미지 분석 + Google Search grounding

- 이미지 1~3장의 일본어 텍스트와 시각적 단서를 추출한다.
- 작품명을 확정하지 않고 검색에 사용할 단서만 반환한다.
- 검색 쿼리와 검색 결과 요약을 함께 반환한다.
- 다음 단계가 사용할 수 있도록 구조화된 JSON으로 반환한다.

AI는 다음과 같은 정보를 찾는다.

일본어 대사
작품명으로 추정되는 문자열
작가명
등장인물 이름
출판사
연재처
해시태그
SNS 게시물 텍스트
작품 식별에 도움이 되는 기타 시각적 단서

단순 OCR만 수행하는 것이 아니라 작품 검색에 필요한 정보를 추출하는 것이 목적이다.

8. 작품 검색

이미지 분석 결과를 바탕으로 일본 작품과 한국 정보를 각각 검색한다.

② Gemini 최종 판정

- ①의 검증된 OCR·단서·검색 결과를 입력으로 받는다.
- 일본 작품 후보 TOP 3과 후보별 한국어 제목·정발 여부·근거를 한 번에 반환한다.
- 정상 요청은 Gemini 호출 2회로 제한한다.

예를 들어 이미지에서:

今日は一緒に帰らない？

山田くん

이 발견됐다면:

"今日は一緒に帰らない" 漫画

"今日は一緒に帰らない" "山田" 漫画

"山田くん" 漫画

등의 검색을 수행할 수 있다.

초기 버전에서는 Gemini의 Google Search Grounding을 활용한다.

9. 작품 후보 선정

② Gemini 최종 판정

하나의 작품을 검색 결과만으로 바로 확정하지 않는다. ② 최종 판정 모델은 다음 구조화된 입력을 종합한다.

- ①의 이미지 분석 결과 JSON
- ①의 검색 결과와 OCR 단서

②는 원본 이미지를 받지 않으며 ①의 구조화된 단서와 검색 결과만 사용한다. 검색 결과에 없는 사실을 새로 추정하지 않으며, 근거가 충돌하거나 부족하면 `UNKNOWN` 또는 `INSUFFICIENT`로 반환한다.

모델 선택 기준은 다음과 같다.

- Pro: 이미지 단서 통합과 최종 후보 비교처럼 추론 품질이 중요한 단계
- Flash: 검색어 처리와 웹 검색처럼 호출 횟수와 비용 효율이 중요한 단계

최종 판정 결과는 다음 정보를 포함한다.

- 최종 후보 TOP 3 순위
- 후보별 `HIGH`, `MEDIUM`, `LOW` 판정 수준
- 후보별 판단 근거
- 한국어 제목 상태와 정발 여부

검색 결과를 바탕으로 하나의 작품을 바로 확정하지 않는다.

가능성이 높은 작품들을 비교한다.

예:

1위 ○○○○
판정 수준 높음

2위 △△△△
판정 수준 낮음

3위 □□□□
판정 수준 낮음

초기 버전에서는 AI가 산출한 숫자를 실제 확률처럼 표시하지 않는다.
검색 품질을 충분히 평가하기 전까지는 `높음`, `중간`, `낮음`과 판단 근거를 표시한다.
숫자형 신뢰도를 도입할 경우에는 별도 평가 데이터로 보정한 뒤 적용한다.

가능하면 다음 요소를 작품 판별 근거로 사용한다.

대사 일치
등장인물 이름 일치
작가명 일치
작품명 일치
이미지 내 텍스트 일치
공식 사이트 정보
출판사 정보
연재처 정보
여러 검색 결과 간 일관성
10. 한국어 제목 검색

작품을 식별한 뒤 한국어 정보를 별도로 조사한다.

여기서 중요한 것은:

한국 정발 제목 ≠ 한국에서 알려진 제목

이라는 점이다.

따라서 한국어 제목을 세 종류로 구분한다.

OFFICIAL

한국에 정식 출판되어 공식 제목이 존재하는 경우.

한국어 제목
향기로운 꽃은 늠름하게 핀다

[정식 제목]

한국 정발 O
COMMON

한국에 정식 출판되지 않았지만 나무위키, 커뮤니티, 블로그 등 한국 웹에서 특정 번역명이 일반적으로 사용되는 경우.

한국어 제목
○○하는 그녀

[국내 통용명]

한국 정발 X
TRANSLATED

한국 정식 제목과 신뢰할 만한 국내 통용명을 모두 찾지 못한 경우.

한국어 참고 제목
○○하는 그녀

[AI 번역]

한국 정발 X

AI가 생성한 번역명을 실제 한국 정식 제목이나 통용명처럼 표시하지 않는다.

`TRANSLATED`와 `UNKNOWN`은 다음 기준으로 구분한다.

- `TRANSLATED`: 일본 원제가 확인되었고 AI가 참고용 한국어 번역 제목을 생성할 수 있지만, 공식 제목이나 국내 통용명은 확인하지 못한 상태
- `UNKNOWN`: 일본 원제 또는 한국어 제목을 신뢰할 수 있게 확인하지 못했거나, 검색 결과가 부족·충돌·실패하여 제목을 생성할 수 없는 상태

`TRANSLATED` 제목은 결과 화면에서 반드시 `[AI 번역]` 또는 `참고 제목`으로 표시한다. `UNKNOWN`인 경우에는 제목을 임의로 만들어 표시하지 않고 `한국어 제목 확인 불충분`으로 표시한다.

한국어 제목 상태와 정발 여부의 허용 조합은 다음과 같이 정의한다.

| 한국어 제목 상태 | `CONFIRMED` | `NOT_FOUND` | `UNKNOWN` |
|---|---:|---:|---:|
| `OFFICIAL` | 허용 | 금지 | 금지 |
| `COMMON` | 금지 | 허용 | 허용 |
| `TRANSLATED` | 금지 | 허용 | 허용 |
| `UNKNOWN` | 허용 | 허용 | 허용 |

`OFFICIAL`은 한국 공식 제목과 정식 발매가 함께 확인된 경우에만 사용한다. 정식 발매는 확인됐지만 공식 한국어 제목을 확인하지 못한 경우에는 `UNKNOWN` + `CONFIRMED`로 반환할 수 있다. `COMMON` 또는 `TRANSLATED` 상태에서 정식 제목이나 정발 사실이 확인되면 `OFFICIAL`로 승격한다.

11. 한국 통용명 탐색

일본 원제가 확인되면 한국 웹에서 해당 작품을 추가로 조사한다.

검색 대상에는 다음과 같은 정보가 포함될 수 있다.

일본 원제
+
한국어
+
만화
+
작가명

그리고 다음과 같은 한국 웹 자료에서 동일한 번역명이 반복적으로 사용되는지를 확인한다.

나무위키
한국 커뮤니티
블로그
SNS
서점
한국 공식 출판사

단, 나무위키 하나만을 절대적인 정답으로 사용하지 않고 여러 출처를 종합하여 판단한다.

국내 통용명(`COMMON`)은 서로 다른 종류의 한국어 자료에서 같은 번역명이 반복 확인될 때만 판정한다.
V1의 최소 기준은 서로 다른 2종류 이상의 자료에서 동일한 제목이 확인되는 것이다.
자료 종류가 하나뿐이거나 제목이 서로 다르면 `COMMON`으로 확정하지 않고 `확인 불충분` 또는 `TRANSLATED`로 처리한다.

검색 결과는 내부 판정에 사용하지만, V1에서는 사용자가 웹 출처 목록을 직접 확인하는 기능을 제공하지 않는다.
출처를 화면에 표시하지 않더라도 근거가 부족한 정보는 확정하지 않고 `확인 불충분`으로 표시한다.

12. 한국 정발 여부

한국에서 실제 정식 출판된 작품인지도 확인한다.

가능하면:

한국 출판사
정식 작품명
한국 서점
공식 판매 정보

등을 근거로 판단한다.

확실한 근거가 없는 경우에는 정발되었다고 단정하지 않는다.

정발 여부는 한국어 제목과 별도로 관리한다.

- `CONFIRMED`: 한국 출판사 또는 정식 판매 정보로 확인됨
- `NOT_FOUND`: 조사한 범위에서 정식 발매 정보를 찾지 못함
- `UNKNOWN`: 검색 결과가 부족하거나 서로 일치하지 않음

`NOT_FOUND`는 미정발 확정이 아니라 조사 범위 내 미확인 상태로 취급한다.

13. 결과 페이지

사용자에게 가장 중요한 한국어 제목을 가장 크게 표시한다.

예:

━━━━━━━━━━━━━━━━━━━━━━━━

          [표지]

    별 아래의 그녀

       星の下の彼女

       [국내 통용명]

━━━━━━━━━━━━━━━━━━━━━━━━

작가
山田太郎

장르
러브코미디 · 학원 · 청춘


🇯🇵 일본 정보

출판사
○○社

연재처
○○ Comics

연재 상태
연재 중


🇰🇷 한국 정보

한국 정발
X

한국어 제목
별 아래의 그녀

제목 상태
국내 통용명


━━━━━━━━━━━━━━━━━━━━━━━━

작품 식별 신뢰도

판정 수준 높음

판단 근거

✓ 이미지 속 대사 일치
✓ 등장인물 이름 일치
✓ 작품 정보 일치

━━━━━━━━━━━━━━━━━━━━━━━━

[ 다른 후보 보기 ]

2위 ○○○○

3위 △△△△
14. 잘못된 검색 결과 처리

AI가 항상 정확하게 작품을 찾을 수 있는 것은 아니다.

따라서 사용자가 결과에 대해:

👍 이 작품이 맞아요

👎 다른 작품이에요

와 같은 피드백을 줄 수 있도록 확장할 수 있다.

잘못된 경우에는 TOP 2, TOP 3 후보를 보여준다.

15. 초기 기술 구성
Frontend
Next.js
TypeScript
Tailwind CSS
PWA
Backend

Next.js Route Handler를 사용한다.

대표 API:

POST /api/identify

요청 계약:

- Content-Type: `multipart/form-data`
- 필드명: `images`
- `images`는 1~3개의 이미지 파일 배열이다.
- 허용 형식: JPG, JPEG, PNG, WebP
- 파일 크기와 해상도는 6.1절의 제한을 따른다.
- 잘못된 요청은 HTTP 400과 구조화된 오류 코드로 응답한다.

성공 응답의 최상위 상태:

- `SUCCESS`: 일본 작품과 한국 정보를 모두 정상적으로 조사함
- `PARTIAL_SUCCESS`: 일본 작품 식별 단계가 `SUCCESS`이고 후보가 1개 이상 존재하지만, 한국어 제목·정발 여부 조사가 실패·시간 초과·정보 부족 상태임. 완료된 일본 작품 후보는 반환하고 실패한 단계의 상태를 함께 반환함
- `INSUFFICIENT`: 작품을 특정할 단서가 부족함
- `FAILED`: 외부 서비스 오류 또는 처리 시간 초과로 결과를 만들지 못함

일본 작품 식별 단계가 `INSUFFICIENT`, `FAILED`, `TIMEOUT`이면 후보를 반환하지 않는다. 이 경우 최상위 상태는 각각 `INSUFFICIENT` 또는 `FAILED`로 처리한다. 장르, 표지, 출판사, 연재처와 같은 후순위 정보만 확인하지 못한 경우에는 일본 작품과 한국 핵심 정보가 완료됐다면 `SUCCESS`를 유지한다.

후보별 판정 수준은 `HIGH`, `MEDIUM`, `LOW` 중 하나이며, 숫자형 확률로 해석하지 않는다.
한국어 제목 상태는 `OFFICIAL`, `COMMON`, `TRANSLATED`, `UNKNOWN` 중 하나다.
정발 여부는 `CONFIRMED`, `NOT_FOUND`, `UNKNOWN` 중 하나다.

단계 상태는 `SUCCESS`, `PARTIAL`, `INSUFFICIENT`, `FAILED`, `TIMEOUT`, `SKIPPED` 중 하나다.

- `imageAnalysis`: 이미지 단서 추출 단계. 실패하면 이후 단계는 `SKIPPED`다.
- `imageAnalysis`: 이미지 OCR·단서 추출 및 Google Search grounding 단계.
- `finalJudgment`: 일본 TOP 3, 한국 제목·정발 여부·근거를 한 번에 반환하는 최종 판정 단계.
- 이전 단계가 `INSUFFICIENT`, `FAILED`, `TIMEOUT`이면 실행하지 않은 다음 단계는 `SKIPPED`다.

최상위 상태와 후보 반환 규칙은 다음과 같다.

- `SUCCESS`: 일본 작품 식별과 한국 핵심 정보 조사가 완료됐으며 후보는 1~3개다.
- `PARTIAL_SUCCESS`: 일본 작품 식별이 `SUCCESS`이고 후보가 1~3개 존재하며, 후보 중 하나 이상에서 한국 핵심 정보 조사가 완료되지 않았다. 일본 작품 후보 TOP 3은 반환하고 후보별 한국 정보 상태를 각각 표시한다.
- `INSUFFICIENT`: 이미지 단서 또는 일본 작품 후보가 부족하다. `candidates`는 빈 배열이다.
- `FAILED`: 핵심 외부 처리 실패로 결과를 만들지 못했다. `candidates`는 빈 배열이다.

`PARTIAL_SUCCESS`의 `candidates`도 최대 3개이며 `rank`는 항상 1부터 연속으로 부여한다. 한국 조사가 완료되지 않은 후보는 `koreanTitle: null`, `koreanTitleStatus: "UNKNOWN"`, `publicationStatus: "UNKNOWN"`으로 반환한다.

판단 근거(`evidence`)는 다음 문자열 enum 중 1개 이상을 담는 배열이다.

- `DIALOGUE_MATCH`: 이미지 속 대사 일치
- `CHARACTER_NAME_MATCH`: 등장인물 이름 일치
- `WORK_TITLE_MATCH`: 작품명 또는 로고 일치
- `AUTHOR_MATCH`: 작가명 일치
- `PUBLISHER_MATCH`: 출판사 정보 일치
- `SERIALIZATION_MATCH`: 연재처 정보 일치
- `VISUAL_CLUE_MATCH`: 캐릭터·복장·장면 등 시각적 단서 일치
- `SEARCH_CONSISTENCY`: 여러 검색 결과 간 일관성

`evidence`는 V1에서 최소 1개, 최대 5개의 문자열 enum을 반환한다. 근거가 1개도 없으면 후보를 생성하지 않는다.

성공 응답 구조:

```json
{
        "status": "SUCCESS",
        "stages": {
                "imageAnalysis": "SUCCESS",
                "japaneseIdentification": "SUCCESS",
                "koreanInvestigation": "SUCCESS"
        },
        "candidates": [
                {
                        "rank": 1,
                        "japaneseTitle": "星の下の彼女",
                        "koreanTitle": "별 아래의 그녀",
                        "koreanTitleStatus": "COMMON",
                        "publicationStatus": "NOT_FOUND",
                        "confidence": "HIGH",
                        "evidence": [
                                "DIALOGUE_MATCH",
                                "CHARACTER_NAME_MATCH"
                        ],
                        "author": "山田太郎"
                }
        ]
}
```

`SUCCESS` 응답에서 `koreanTitle`은 문자열이며 `koreanTitleStatus`와 함께 반드시 반환한다. `publicationStatus`도 반드시 반환하며, 확인할 수 없으면 `UNKNOWN`으로 반환한다. `evidence`는 위 enum의 문자열 배열이다.

부분 성공 응답 예시:

```json
{
        "status": "PARTIAL_SUCCESS",
        "stages": {
                "imageAnalysis": "SUCCESS",
                "japaneseIdentification": "SUCCESS",
                "koreanInvestigation": "TIMEOUT"
        },
        "candidates": [
                {
                        "rank": 1,
                        "japaneseTitle": "星の下の彼女",
                        "koreanTitle": null,
                        "koreanTitleStatus": "UNKNOWN",
                        "publicationStatus": "UNKNOWN",
                        "confidence": "HIGH",
                        "evidence": ["DIALOGUE_MATCH"],
                        "author": "山田太郎"
                }
        ]
}
```

`PARTIAL_SUCCESS`에서는 한국 정보가 완료된 후보와 완료되지 않은 후보가 함께 반환될 수 있다. 각 후보의 `koreanTitle`은 정보가 없을 때만 `null`이며, 필드는 생략하지 않는다. `koreanTitleStatus`와 `publicationStatus`는 이 경우 모두 `UNKNOWN`이어야 한다.

오류 응답:

```json
{
        "error": {
                "code": "INVALID_IMAGE",
                "message": "업로드한 이미지를 처리할 수 없습니다.",
                "retryAfterSeconds": 10
        }
}
```

오류 코드는 `INVALID_IMAGE`, `RATE_LIMITED`, `TIMEOUT`, `INSUFFICIENT_CLUES`, `UPSTREAM_UNAVAILABLE`, `INTERNAL_ERROR`를 사용한다.
응답에 API 키, 내부 프롬프트, 스택 트레이스, 외부 서비스의 상세 오류를 포함하지 않는다.

역할:

이미지 수신
→ 이미지 검증
→ Gemini 호출
→ 이미지 분석
→ 검색
→ 작품 후보 선정
→ 한국 정보 조사
→ 결과 반환
AI

Gemini API 모델 역할:

- `gemini-3.8-flash`: 이미지 분석 전용

① 이미지 이해 및 단서 추출
② 일본 작품 후보 검색
③ 한국어 제목 및 정발 정보 검색
④ 검색 결과 기반 최종 판정

검색

Google Search Grounding (`google_search` 도구)

초기 버전에서는 별도의 검색 API를 추가하지 않고 Gemini의 검색 기능을 우선 활용한다.

각 단계의 모델 응답은 서버에서 JSON Schema 또는 동등한 검증 방식으로 확인한다. 모델이 반환한 필드가 enum 또는 필수 필드 조건을 만족하지 않으면 해당 단계는 실패로 처리하고 최종 결과에 추정값을 포함하지 않는다.


16. API Key 관리

Gemini API Key를 프론트엔드에 노출하지 않는다.

❌ Browser
     ↓
Gemini API

대신:

📱 Browser

     ↓ HTTPS

Azure Container Apps
Next.js Server

     ↓

Gemini API

구조를 사용한다.

Gemini API Key는 Azure의 환경변수/Secret을 통해 서버에 전달한다.

17. 이미지 처리 원칙

초기 버전에서는 사용자가 업로드한 캡처 이미지를 DB에 영구 저장하지 않는다.

이미지 업로드

↓

분석

↓

Gemini 전달

↓

작품 검색

↓

결과 반환

↓

서버에 이미지 영구 저장 X

추후 사용자가 직접 검색 기록 저장 기능을 활성화할 경우 별도의 정책을 설계한다.

18. V1에서 제외할 기능

첫 번째 버전에서는 다음 기능을 의도적으로 제외한다.

회원가입
Google 로그인
DB
친구 기능
댓글
자체 AI 모델 학습
별도 OCR 모델
자체 검색엔진
복잡한 크롤링
사용자 이미지 영구 저장
관리자 페이지

첫 번째 목표는 하나다.

SNS 캡처 이미지를 넣었을 때 실제 작품을 얼마나 정확하게 찾아낼 수 있는가?

19. V1 기능 범위
필수
PWA 설치
모바일 UI
이미지 1~3장 업로드
Gemini 이미지 분석
일본어 텍스트 추출
작품 검색
작품 후보 TOP 3
한국어 제목
일본 원제
작가
국내 정발 여부
정식명 / 통용명 / AI 번역 구분
작품 식별 근거
Docker 컨테이너화
Azure 배포
가능하면 추가
작품 표지
장르
일본 출판사
연재처
연재 상태
20. V2 확장

V1의 작품 검색 정확도가 충분한 것을 확인한 후 진행한다.

DB를 추가하여:

Google 로그인
검색 기록
최근 찾은 작품
찜
내 만화 목록
기기 간 동기화

등을 구현한다.

DB는 프로젝트 방향에 따라 Azure Database for PostgreSQL 등을 검토할 수 있다.

21. V3 확장

검색 정확도와 작품 정보 품질을 높인다.

예를 들어:

검색 Query 다중 생성
별도 검색 API 추가
공식 출판사 우선 검증
일본 공식 연재 사이트 검증
한국 통용명 판별 알고리즘 개선
사용자 피드백을 활용한 검색 개선
권수 정보
발매일
작품 표지
연재 상태 자동 확인

등을 추가한다.

22. 최종 시스템 구조
                   📱 사용자
                       │
                  MangaFind PWA
                       │
                       ▼
             Azure Container Apps
                       │
                Docker Container
                       │
               Next.js Server
                       │
                       ▼
                                          Gemini 2.5 Pro
                                                  ① 이미지 분석
                                                          │
                                                          ▼
                 Gemini 2.5 Flash + Google Search
                                 ② 일본 작품 후보 검색
                                                          │
                                                  │
                                                  ▼
                          ③ 한국 정보 검색
                          Gemini 2.5 Flash
                          + Google Search
                                                  │
                                                  ▼
                                        검색 결과·단서 통합
                                                  │
                                                  ▼
                                          ④ 최종 판정
                                          Gemini 2.5 Pro
                                                  │
                                                  ▼
                                              결과 생성
                                                          │
                                                          ▼
                                                  📱 사용자
23. 프로젝트 핵심 원칙

이 프로젝트를 개발하면서 가장 중요하게 가져갈 원칙은 세 가지야.

첫째, AI의 기억보다 검색 결과를 신뢰한다. Gemini가 작품을 알고 있다고 바로 답하게 하지 않고, 이미지에서 단서를 추출하고 실제 웹 검색으로 검증한다.

둘째, 한국어 제목의 판정 상태를 구분한다. 한국 정식명, 국내 통용명, AI 참고 번역을 명확하게 구별하고, 각 판정은 내부 검색 근거가 충분할 때만 확정한다.

셋째, V1은 검색 정확도에 집중한다. 로그인이나 DB 같은 부가 기능보다 “캡처 한두 장만으로 내가 찾던 만화를 실제로 찾아주는가?”를 먼저 완성한다.

24. 비판적 검토 반영 사항

24.1 V1 성공 기준

V1은 기능 구현 여부만으로 성공을 판단하지 않는다. 최소 20~30개의 대표 캡처 샘플을 준비하고 다음 지표를 측정한다.

- Top 1 작품 식별 정확도
- Top 3 후보 안에 정답이 포함되는 비율
- 잘못된 작품을 높은 판정 수준으로 표시하는 비율
- 한국어 제목 상태 판정 정확도
- 검색 요청의 p95 응답 시간
- 요청 1건당 평균 및 최대 AI 비용

V1 통과 기준은 다음과 같다.

- Top 1 작품 식별 정확도 70% 이상
- Top 3 후보 안에 정답이 포함되는 비율 85% 이상
- 높은 판정 수준으로 표시한 후보의 오판률 15% 이하
- 한국어 제목 상태 판정 정확도 70% 이상
- 전체 2회 호출 요청 p95 응답 시간 60초 이하
- 요청 1건당 평균 및 최대 AI 비용은 사전 비용 측정 후 운영 예산 안에서 승인한다.

평가 결과가 기준에 미달하면 기능을 추가하기보다 검색 파이프라인을 개선한다. 샘플 수가 20개 미만이면 출시 판단에 사용하지 않는다.

24.2 검색 파이프라인과 불확실성 처리

다음 단계를 논리적으로 분리한다.

이미지 단서 추출
→ 일본 작품 후보 검색
→ 후보별 단서 비교
→ 한국어 정보 조사
→ 결과 생성

각 단계는 `성공`, `정보 부족`, `외부 검색 실패`, `처리 시간 초과` 상태를 구분한다. 검색 결과가 부족한 경우 임의의 작품을 채우지 않고 사용자가 다른 이미지를 업로드하도록 안내한다.

24.3 공개 API 및 비용 제한

회원가입이 없는 V1에서는 누구나 API를 호출할 수 있으므로 다음 제한을 적용한다.

- IP 기준 분당 최대 3회, 1일 최대 30회
- 이미지 개수, 파일 크기, 총 픽셀 수 제한
- 동시 처리 요청 수 제한
- 외부 AI 호출은 서버에서 자동 재시도하지 않는다.
- 일시적인 오류나 timeout은 해당 단계의 실패로 기록하고, 사용자가 새 요청으로 다시 시도한다.
- 전체 요청 시간 제한
- 비용 또는 사용량 초과 시 일시적인 요청 차단

rate limit 초과 시 HTTP 429와 `Retry-After` 헤더를 반환한다.

오류 응답에는 내부 프롬프트, API 키, 외부 서비스의 상세 오류를 포함하지 않는다.

24.4 이미지 업로드 보안

클라이언트 검증은 편의를 위한 것이며 보안 경계로 사용하지 않는다. 서버에서 실제 이미지 디코딩 여부, MIME 형식, 파일 크기, 해상도와 픽셀 수를 다시 확인한다.

분석 전에 EXIF 등 불필요한 메타데이터를 제거하고, 업로드 파일을 실행 파일처럼 처리하지 않는다. 원본 이미지는 분석 요청이 끝난 뒤 삭제하며, 로그·분석 추적 정보·오류 메시지에 이미지 원본이나 민감한 내용을 남기지 않는다.

24.5 요청 처리 방식

V1은 동기식 `POST /api/identify`로 구현한다.

- 각 외부 처리 단계의 timeout: 12초
- 전체 요청 timeout: 60초
- 서버 자동 재시도: 없음

처리 예산은 다음과 같이 계산한다. 각 단계는 한 번만 호출하며, 서버 자동 재시도는 전체 시간 예산에 포함하지 않는다.

- ① Gemini 2.5 Pro 이미지 분석: 최대 12초
- ② Gemini 2.5 Flash + Google Search 일본 작품 검색: 최대 12초
- ② Gemini 최종 판정: 최대 12초
- 업로드 검증·응답 변환·네트워크 여유: 최대 12초

두 Gemini 호출은 순차 실행하며 최대 합계는 24초다. 업로드 검증·응답 변환·네트워크 여유 36초를 포함해 전체 60초를 넘지 않아야 한다. 이미지 분석이나 최종 판정이 제한 시간을 넘으면 부분적으로 추정한 결과를 성공으로 반환하지 않고 `TIMEOUT` 또는 `FAILED` 상태를 반환한다.

향후 처리 시간이 길어지면 작업 ID를 반환하는 비동기 방식으로 전환할 수 있도록 AI 파이프라인을 Route Handler와 분리한다.

24.6 비용 및 timeout 재측정

2회 호출 파이프라인 도입 후 출시 전 30건의 실제 요청으로 비용과 처리 시간을 다시 측정한다. 샘플은 이미지 1장, 2장, 3장 요청을 각각 10건씩 구성하고, 정상 성공·검색 불충분·최종 판정 실패 사례를 포함한다.

각 요청에서 다음 값을 기록한다.

- 단계별 시작·종료 시각과 전체 처리 시간
- Gemini 2.5 Pro 호출 횟수와 입력·출력 토큰 수
- Gemini 2.5 Flash 호출 횟수와 입력·출력 토큰 수
- ②와 ③의 Google Search 호출 여부 및 검색 호출 비용
- 내부 재시도 횟수와 재시도로 증가한 시간·비용
- 성공, 부분 성공, 정보 부족, 실패 상태

기본 호출 비용은 `Pro 이미지 분석 1회 + Flash 일본 검색 1회 + Flash 한국 검색 1회 + Pro 최종 판정 1회`로 계산한다. ④에는 원본 이미지를 재전송하지 않고 ①의 구조화 JSON만 전달하므로 최종 판정 단계의 이미지 입력 비용은 발생하지 않는다. 사전 측정 없이 평균 비용을 추정하지 않는다.

측정 결과는 다음 기준으로 출시 여부를 판단한다.

- 전체 처리 시간 p95가 60초 이하인가
- 단계별 timeout이 반복적으로 발생하지 않는가
- 1건당 평균·최대 비용이 승인된 운영 예산 이하인가
- 1장·2장·3장 요청 간 비용과 시간 증가폭이 감당 가능한가

기준을 충족하지 못하면 이미지 해상도, 검색 후보 수, 재시도 횟수 또는 모델 호출 프롬프트를 조정한 뒤 동일한 30건 측정을 반복한다. 실제 API 키와 결제 프로젝트 없이 산출한 금액은 예상치로 취급하고 출시 비용 기준으로 사용하지 않는다.

24.7 제목과 정발 정보의 독립성

한국어 제목 상태와 정발 여부는 서로 다른 정보로 취급한다.

- 공식 한국어 제목
- 국내 통용 제목
- AI 참고 번역 제목
- 정발 여부 상태
- 각 정보의 내부 판정 근거와 확신 수준

AI 번역 제목은 공식 제목이나 국내 통용명으로 승격하지 않는다. 검색 근거가 충돌하거나 부족하면 제목과 정발 여부 모두 `확인 불충분`으로 표시할 수 있다.

24.8 개인정보 및 저작권 안내

사용자가 업로드한 이미지가 외부 AI 서비스로 전송될 수 있음을 업로드 전에 안내한다. 분석 완료 후 이미지 보존 여부와 삭제 시점을 고지하며, 외부 모델 제공자의 데이터 학습 사용 정책을 확인한다.

작품 표지나 외부 이미지는 V1에서 선택 기능으로 두고, 표시할 경우 이미지 라이선스와 외부 URL 의존성을 별도로 검토한다.

25. V1 범위 조정

V1의 최우선 목표는 이미지에서 일본 작품 후보를 정확하게 찾는 것이다. 다음 기능은 검색 품질을 해치지 않는 범위에서만 제공한다.

필수:

- 이미지 1~3장 업로드
- 일본어 단서 추출
- 일본 작품 후보 TOP 3
- 일본 원제와 작가
- 판단 근거와 판정 수준
- 확인 가능한 경우의 한국어 제목과 정발 상태
- 모바일 웹 사용성
- Docker 컨테이너화 및 Azure 배포

후순위:

- PWA 설치 최적화
- 작품 표지
- 장르, 출판사, 연재처, 연재 상태
- 국내 통용명 자동 판정 고도화

V1에서는 결과에 웹 출처 목록을 표시하지 않는다. 대신 내부 검색 결과를 판정에 사용하고, 근거가 부족한 경우 확정 결과를 생성하지 않는다.